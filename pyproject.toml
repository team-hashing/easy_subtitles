[project]
name = "easy-subtitles"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai-whisper>=20250625",
]

[project.scripts]
"easysubs" = "main:main"
